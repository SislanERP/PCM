// No se esta ejecutando este codigo por que hay una rediección en el Controller

if (<%= @master_maintenance_plan.errors.blank? %>) {

  Turbolinks.visit('<%= "#{company_device_types_url}" %>')
  console.log('<%= "#{company_device_types_url}" %>')

  $(function () {
    toastr.options = notification_options
    toastr.success('Borrado satisfactorimente', " " )
  });

} else {
  $(function () {
    toastr.options = notification_options;
    toastr.error('<%= @master_maintenance_plan.errors.get(:base) %>', "Error" );
  });
}
